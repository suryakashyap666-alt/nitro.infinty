version: "3.8"

services:
  ai-backend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      - NITRO_SYSTEM_API_KEY=${NITRO_SYSTEM_API_KEY:-}
      - WEB_CLIENT_ORIGIN=*
    restart: unless-stopped