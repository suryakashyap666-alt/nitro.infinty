{
  "name": "nitro-infinity-ai",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd web-client && npm install && npm run build"
  },
  "dependencies": {
    "firebase": "^12.17.1"
  }
}
